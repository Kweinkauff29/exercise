<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Excersise WebApp Homepage</title>
    <!-- Add the Bootstrap CSS file from a CDN -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.7.3/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

<!-- Add the Bootstrap JavaScript file from a CDN -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.7.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
  </head>
  <body>

    <style media="screen">
      body {
        margin: 0 !important;
      }
      .head-img {
        display: flex;
        justify-content: center;
        align-items: center;
      }
      #headhr {
        color: black;
        background-color: black;
        height: 5px;
        animation: grow linear 1s;
      }
      #exctrakimg {
        height: 115px;
      }

      @keyframes grow {
        from {
          transform: scale(0);
        }
        to {
          transform: scale(1);
        }
      }

      /* Style the hamburger icon */
      .hamburger {
        width: 30px;
        height: 20px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        cursor: pointer;
      }

      .line {
        width: 100%;
        height: 2px;
        background-color: black;
      }

      .hamburger-container {
        display: inline-flex;

      }

      /* Style the dropdown tab */
      .dropdown {
        justify-content: center;
        display: none; /* Hidden by default */
      }

      /* Show the dropdown when the hamburger is clicked */
      .hamburger:hover + .dropdown, .hamburger:focus + .dropdown {
        display: inline;
      }

      .dropdown a {
        display: grid;
        text-decoration: none;
        color: black;
      }

      .dropdown a:hover, .dropdown a:focus {
        background-color: #eee;
      }

      /* Transform the hamburger into an "x" when it is clicked */
      .hamburger.active {
        transform: rotate(45deg);
      }

      .hamburger.active .line:first-child {
        transform: translateY(7px) rotate(90deg);
      }

      .hamburger.active .line:nth-child(2) {
        opacity: 1;
        width: 33px;
      }

      .hamburger.active .line:last-child {
        transform: translateY(-7px) rotate(90deg);
      }

      .logincontainer {
        display: grid;
        justify-content: center;
        text-align: center;
      }

    </style>

    <div class="head-img">
      <img id="exctrakimg" src="https://www.bonitaesterorealtors.com/wp-content/uploads/2022/12/Affiliate-Spotlight-8-10.png" alt="">

      <div class="hamburger-container" onclick="toggleMenu()">
        <!-- The hamburger icon -->
        <div class="hamburger" onclick="hamurger()">
          <div class="line"></div>
          <div class="line"></div>
          <div class="line"></div>
        </div>
      </div>
    </div>

    <hr id="headhr">


            <!-- The dropdown tab -->
            <div class="dropdown">
              <a href="#">Menu item 1</a>
              <hr style="width: 100%;">
              <a href="#">Menu item 2</a>
              <hr style="width: 100%;">
              <a href="#">Menu item 3</a>
            </div>

    <script type="text/javascript">
      //This opens and closes the hamburger icon showing the dropdown tab
      let ham = 0;
      let drop = document.querySelector('.dropdown');
      function hamurger() {
        if (ham == 0) {
          drop.style.display = "grid";
          ham++;
        }
        else if (ham == 1) {
          drop.style.display = "none";
          ham = 0;
        }
        else (err);
      }

      function toggleMenu() {
      // Get the hamburger element
      var hamburger = document.querySelector('.hamburger');

      // Toggle the "active" class on the hamburger
      hamburger.classList.toggle('active');
    }
    </script>

    <div class="logincontainer">
      <h1>Login:</h1>
      <form class="login" method="post">
        <label for="name">Username:</label>
        <input type="text" id="name" name="name">
        <br>
        <br>
        <label for="password">Password:</label>
        <input type="text" id="password" name="password">
        <br>
        <br>
        <!-- A submit button -->
        <input type="submit" value="Submit">
      </form>
      <p style="color: red;">Login Failed - Please Try again or create an account.</p>
    </div>

  </body>
</html>
