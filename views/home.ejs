<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Excersise WebApp Homepage</title>
    <!-- Add the Bootstrap CSS file from a CDN -->
  <!-- Add the Bootstrap JavaScript file from a CDN -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>
  <body>

    <style media="screen">
      body {
        margin: 0 !important;
      }
      .head-img {
        display: flex;
        justify-content: center;
        align-items: center;
      }
      #headhr {
        color: black;
        background-color: black;
        height: 5px;
        animation: grow linear 1s;
      }
      #exctrakimg {
        height: 115px;
      }

      @keyframes grow {
        from {
          transform: scale(0);
        }
        to {
          transform: scale(1);
        }
      }



      /* Style the hamburger icon */
      .hamburger {
        width: 30px;
        height: 20px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        cursor: pointer;
      }

      .line {
        width: 100%;
        height: 2px;
        background-color: black;
      }

      .createaccbtn {
        background-color: #74e2dc;
      }

      .createaccbtn:hover {
        cursor: pointer;
        background-color: #5fc0bb;
      }

      .hamburger-container {
        display: inline-flex;

      }

      /* Style the dropdown tab */
      .dropdown {
        justify-content: center;
        display: none; /* Hidden by default */
        background-color: grey;
      }

      /* Show the dropdown when the hamburger is clicked */
      .hamburger:hover + .dropdown, .hamburger:focus + .dropdown {
        display: inline;
      }

      .createaccount {
        display: none;
      }

      .dropdown a {
        display: grid;
        text-decoration: none;
        color: black;
      }

      .dropdown a:hover, .dropdown a:focus {
        background-color: #eee;
      }

      /* Transform the hamburger into an "x" when it is clicked */
      .hamburger.active {
        transform: rotate(45deg);
      }

      .hamburger.active .line:first-child {
        transform: translateY(7px) rotate(90deg);
      }

      .hamburger.active .line:nth-child(2) {
        opacity: 1;
        width: 33px;
      }

      .hamburger.active .line:last-child {
        transform: translateY(-7px) rotate(90deg);
      }

      .logincontainer {
        display: grid;
        justify-content: center;
        text-align: center;
      }

    </style>

    <div class="head-img">
      <img id="exctrakimg" src="https://www.bonitaesterorealtors.com/wp-content/uploads/2022/12/Affiliate-Spotlight-8-10.png" alt="">

      <div class="hamburger-container" onclick="toggleMenu()">
        <!-- The hamburger icon -->
        <div class="hamburger" onclick="hamurger()">
          <div class="line"></div>
          <div class="line"></div>
          <div class="line"></div>
        </div>
      </div>
    </div>

    <hr id="headhr">


            <!-- The dropdown tab -->
            <div class="dropdown">
              <a href="#">Menu item 1</a>
              <hr style="width: 100%;">
              <a href="#">Menu item 2</a>
              <hr style="width: 100%;">
              <a href="#">Menu item 3</a>
            </div>

    <script type="text/javascript">
      //This opens and closes the hamburger icon showing the dropdown tab
      let ham = 0;
      let drop = document.querySelector('.dropdown');
      function hamurger() {
        if (ham == 0) {
          drop.style.display = "grid";
          ham++;
        }
        else if (ham == 1) {
          drop.style.display = "none";
          ham = 0;
        }
        else (err);
      }

      function toggleMenu() {
      // Get the hamburger element
      var hamburger = document.querySelector('.hamburger');

      // Toggle the "active" class on the hamburger
      hamburger.classList.toggle('active');
    }
    </script>

    <div class="logincontainer">
      <h1>Login:</h1>
      <form class="login" action="/" method="post">
        <label for="name">Username:</label>
        <input type="text" id="name" name="name">
        <br>
        <br>
        <label for="password">Password:</label>
        <input type="text" id="password" name="password">
        <br>
        <br>
        <!-- A submit button -->
        <input type="submit" value="Submit">
      </form>
      <hr style="width: 100%; margin: 20px auto;">
      <h1>New to ExerTrak?</h1>
      <button type="button" class="btn btn-primary createaccbtn" name="button" onclick="showCreate()">Create Your Account</button>
      <div class="createaccount">
        <form class="login" action="/main/:name" method="post">
          <label for="name">Username:</label>
          <input type="text" id="name" name="name">
          <br>
          <br>
          <label for="password">Password:</label>
          <input type="text" id="password" name="password">
          <br>
          <br>
          <label for="email">Email:</label>
          <input type="text" id="email" name="email">
          <br>
          <br>
          <label for="email">Current Weight:</label>
          <input type="text" id="weight" name="weight">
          <br>
          <br>
          <!-- A submit button -->
          <input type="submit" value="Submit">
        </form>
      </div>
      <p style="color: red; margin: 20px auto;"><%= message %></p>
    </div>


    <!-- JS script to show account creation field-->
    <script type="text/javascript">
    function showCreate() {
      document.querySelector('.createaccbtn').style.display = "none";
      document.querySelector('.createaccount').style.display = "grid";
    }
    </script>

  </body>
</html>
